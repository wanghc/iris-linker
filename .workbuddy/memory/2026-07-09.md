- 修改 package.json 中命令 `iris-linker.exportToXML` 的菜单标题：`Export File to XML` → `Export IRIS File to XML`（用户要求导出 xml 菜单描述加上 IRIS 标识）。
- 在 Git 源代码管理面板的文件右键菜单（menu `scm/resourceState/context`，when=`scmProvider == git && resourceExtname =~ ...`）增加“Export IRIS File to XML”入口。
- 同步修改 `src/extension.ts` 的 exportToXML 命令：兼容 Git 面板传入的 `SourceControlResourceState`（取 `.resourceUri`），并把 `git:` 协议 URI 转成 `file:` 以便计算工作区相对路径与读取 objectscript.conn 配置。类型检查通过。
- 新增「Staged Changes 批量导出」：在 `scm/resourceGroup/context`（Git 暂存组 id=`index`）右键增加“Export IRIS File (N) to XML”，动态数量 N 由定时器轮询 `git diff --cached --name-only` 过滤 IRIS 类型后写入上下文键 `irisStagedCount`（每 2s，上限 30，溢出显示 `(30+)`）；为此预注册 `iris-linker.exportStagedToXML_1.._30` 共 30 个命令，菜单用 `when: scmProvider == git && scmResourceGroup == index && irisStagedCount == N` 只显示当前数量项。点击后弹目录选择框，批量把全部暂存 IRIS 文件分别导出为 XML（复用 `getDocumentNameFromGitPath`/`getSuggestedFileNameFromGitPath`/`exportToXMLContent`）。类型检查通过。
- 注意：VS Code contrib 菜单标题不支持动态插值，故“(N)”只能用 setContext+预注册多命令的变通方案实现。
- 用户决定不用轮询动态数量方案。已回退：删除 30 个 `iris-linker.exportStagedToXML_1.._30` 命令、30 条轮询 menu 项与 `irisStagedCount` 上下文/定时器逻辑；改为单个固定命令 `iris-linker.exportStagedToXML`（菜单标题固定 `Export IRIS File to XML`，when=`scmProvider == git && scmResourceGroup == index`）。版本号升至 `0.0.11`。类型检查通过、无残留引用。
- 在 Git 面板「Changes（未暂存）」组加上同样功能：① 文件右键已覆盖——`scm/resourceState/context` 菜单无 `scmResourceGroup` 限制，原本就同时作用于 Changes 与 Staged 文件；② 新增组右键命令 `iris-linker.exportChangesToXML`（菜单 `scm/resourceGroup/context`，when=`scmProvider == git && scmResourceGroup == workingTree`）。`src/extension.ts` 将 Staged/Changes 批量导出合并为共用 `runBatchExport(isStaged)`：Staged 取 `git diff --cached --name-only`，Changes 取 `git diff --name-only` + `git ls-files --others --exclude-standard`（含未跟踪）。版本号 0.0.11 不变。类型检查通过。
- 将 v0.0.11 的全部改动（菜单改名、Git 面板文件/Staged/Changes 三组右键导出）写入 `CHANGELOG.md` 顶部新增 `[0.0.11]` 小节。