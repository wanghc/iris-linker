# 2026-06-22 工作日志

## Export to XML 导出为空问题排查 - 增加日志
- 修改 `src/tool/tool.ts` 的 `exportToXMLContent` 函数：增加 outputChannel 参数，添加详细日志输出（API URL、docName、response 结构、content 类型/长度/预览）
- 修改 `src/extension.ts` 的 exportCommand：传入 outputChannel 并在关键步骤添加日志（uri 信息、服务器配置、docName 计算、导出内容长度、空内容警告）
- 当导出结果为空时，主动弹出 WarningMessage 提示用户查看 IRIS Linker 输出面板
- 构建成功，但 vsix 打包因沙箱权限限制失败，需用户在沙箱外手动执行 `npm install @vscode/vsce --save-dev && npx vsce package --allow-missing-repository`
